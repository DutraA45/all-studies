class TodoItem {
  int id;
  int userId;
  String title;
  bool completed;

  TodoItem(
      {required this.id,
      required this.userId,
      required this.title,
      required this.completed});
}
